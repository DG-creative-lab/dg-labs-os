---
import DesktopLayout from '../layouts/DesktopLayout.astro';
import DesktopWorkspace from '../components/global/DesktopWorkspace';
import {
  activeNetworkModule,
  activeProfile,
  activeProfileModules,
  activeWritingModule,
} from '../profiles';
---

<DesktopLayout
  profile={activeProfile}
  title={activeProfile.seo.title}
  description={activeProfile.seo.description}
  canonical={activeProfile.contact.website}
  openGraph={{
    url: activeProfile.contact.website,
    title: activeProfile.seo.title,
    description: activeProfile.seo.description,
    site_name: activeProfile.identity.displayName,
  }}
>
  <DesktopWorkspace
    client:load
    profile={activeProfile}
    modules={activeProfileModules}
    network={activeNetworkModule}
    writing={activeWritingModule}
  />
</DesktopLayout>
