---
import DesktopLayout from '../../layouts/DesktopLayout.astro';
import WindowFrame from '../../components/global/WindowFrame.astro';
import { userConfig } from '../../config/index';
import { networkNodes, networkLinks } from '../../config/network';
import NetworkApp from '../../components/network/NetworkApp';
---

<DesktopLayout>
  <main class="mx-auto max-w-5xl px-6 pt-16 text-white">
    <WindowFrame title="Network" closeHref="/desktop">
      <div class="flex items-start justify-between gap-6">
        <div>
          <h1 class="text-2xl font-semibold">Network</h1>
          <p class="mt-2 text-white/70">
            A map of roles, systems, and ideas that reinforce each other: agency-first design,
            learning loops, and infrastructure.
          </p>
        </div>
        <div class="hidden md:block text-right text-xs text-white/50">
          <p>DG-Labs OS</p>
          <p>Module: Network</p>
        </div>
      </div>

      <div class="mt-4 flex flex-wrap gap-2">
        <a
          class="text-sm rounded-full bg-white/10 px-3 py-1.5 hover:bg-white/20 transition"
          href={userConfig.social.linkedin}
          target="_blank"
          rel="noopener noreferrer"
        >
          LinkedIn
        </a>
        <a
          class="text-sm rounded-full bg-white/10 px-3 py-1.5 hover:bg-white/20 transition"
          href={userConfig.social.github}
          target="_blank"
          rel="noopener noreferrer"
        >
          GitHub
        </a>
        <a
          class="text-sm rounded-full bg-white/10 px-3 py-1.5 hover:bg-white/20 transition"
          href={networkLinks.githubOrg}
          target="_blank"
          rel="noopener noreferrer"
        >
          ai-knowledge-hub (org)
        </a>
        <a
          class="text-sm rounded-full bg-white/10 px-3 py-1.5 hover:bg-white/20 transition"
          href={networkLinks.githubPersonal}
          target="_blank"
          rel="noopener noreferrer"
        >
          DG-creative-lab
        </a>
        <a
          class="text-sm rounded-full bg-white/10 px-3 py-1.5 hover:bg-white/20 transition"
          href={networkLinks.newsHub}
          target="_blank"
          rel="noopener noreferrer"
        >
          AI News Hub
        </a>
      </div>

      <NetworkApp client:load nodes={networkNodes} />
    </WindowFrame>
  </main>
</DesktopLayout>
